This module is tests.lambda_effects.

To fetch_data with url, produce. It performs io:
  Return io.get(url).

To process_with_lambda, produce. It performs io:
  Let fetcher be function with endpoint, produce:
    Return io.get(endpoint).
  Return fetcher("/api/data").
