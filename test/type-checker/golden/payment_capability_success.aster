This module is test.typechecker.payment_success.

// Valid Payment capability usage - should produce no errors

To processPaymentValid with orderId, amount, produce. It performs io [Payment]:
  Return ok of Payment.charge(orderId, amount).

To refundPaymentValid with paymentId, produce. It performs io [Payment]:
  Return ok of Payment.refund(paymentId).
