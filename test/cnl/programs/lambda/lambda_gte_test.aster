This module is demo.lambdagte.

To isAtLeast with x, threshold, produce:
  Return x at least threshold.

To isLengthAtLeast with s, minLen, produce:
  Let len be Text.length(s).
  Return len at least minLen.
