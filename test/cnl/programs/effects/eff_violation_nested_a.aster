This module is test.eff.violation.nested_a.

To inner_io, produce. It performs io [Http]:
  Return Http.get("/ping").

To middle_cpu, produce. It performs cpu:
  Return inner_io().

To outer_pure, produce:
  Return middle_cpu().
