// 中文函数 - 函数定义和调用
【模块】测试.函数。

【定义】结果 包含 成功，信息。

// 单参数简单函数
翻倍 入参 数值，产出：
  返回 数值 乘 2。

// 多参数函数
相加 入参 甲，乙，产出：
  返回 甲 加 乙。

// 调用其他函数的函数
四倍 入参 数值，产出：
  令 双倍 为 翻倍(数值)。
  返回 翻倍(双倍)。

// 返回记录的函数
创建结果 入参 成功，信息，产出：
  返回 「结果已生成」。

// 带局部变量的函数
计算 入参 基数，倍数，偏移，产出：
  令 乘积 为 基数 乘 倍数。
  令 结果 为 乘积 加 偏移。
  返回 结果。
