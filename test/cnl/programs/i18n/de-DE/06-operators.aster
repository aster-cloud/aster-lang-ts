// Deutsche Operatoren - Arithmetische und Vergleichsoperatoren
Dieses Modul ist test.operatoren.

// Arithmetische Operationen
Um zahlenAddieren mit a, b, liefert:
  gib zurueck a plus b.

Um zahlenSubtrahieren mit a, b, liefert:
  gib zurueck a minus b.

Um zahlenMultiplizieren mit a, b, liefert:
  gib zurueck a mal b.

Um zahlenDividieren mit a, b, liefert:
  gib zurueck a geteilt durch b.

// Vergleichsoperationen
Um istGroesser mit a, b, liefert:
  gib zurueck a groesser als b.

Um istKleiner mit a, b, liefert:
  gib zurueck a kleiner als b.

Um sindGleich mit a, b, liefert:
  gib zurueck a entspricht b.

// Boolesche Operationen
Um beideWahr mit a, b, liefert:
  wenn a entspricht wahr:
    wenn b entspricht wahr:
      gib zurueck wahr.
  gib zurueck falsch.

Um einesWahr mit a, b, liefert:
  wenn a entspricht wahr:
    gib zurueck wahr.
  wenn b entspricht wahr:
    gib zurueck wahr.
  gib zurueck falsch.

Um negieren mit a, liefert:
  gib zurueck nicht a.

// Kombinierter Ausdruck
Um komplexeBerechnung mit a, b, c, liefert:
  sei summe gleich a plus b.
  sei produkt gleich summe mal c.
  sei resultat gleich produkt minus a.
  gib zurueck resultat.
