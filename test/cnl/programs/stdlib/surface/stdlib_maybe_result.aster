This module is std.maybe_result.

To inc with x, produce:
  Return x.

To maybeWithDefault with x, d, produce:
  Return Maybe.withDefault(x, d).

To mapMaybe with x, produce:
  Return Maybe.map(x, inc).

To logErr with e, produce:
  Return e.

To mapOk with r, produce:
  Return Result.mapOk(r, inc).

To tapError with r, produce:
  Return Result.tapError(r, logErr).
