This module is test.pii.nested_call.

To format_user with ssn, produce:
  Return "SSN: " plus ssn.

To upload_data with data, produce. It performs io [Http]:
  Return Http.post("/upload", data).

To process_ssn with ssn, produce. It performs io [Http]:
  Let formatted be format_user(ssn).
  Return upload_data(formatted).
