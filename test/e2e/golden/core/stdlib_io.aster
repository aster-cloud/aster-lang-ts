This module is std.io.

To print with msg, produce. It performs io:
  Return IO.print(msg).

To readLine, produce. It performs io:
  Return IO.readLine().

To readFile with path, produce. It performs io:
  Return IO.readFile(path).

To writeFile with path, contents, produce. It performs io:
  Return IO.writeFile(path, contents).
